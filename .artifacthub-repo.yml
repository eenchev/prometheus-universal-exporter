# Artifact Hub repository ownership metadata.
#
# Artifact Hub reads this file from the repository the chart is published from
# and uses it to verify ownership: a repository claiming a repositoryID that is
# not theirs is not indexed. It is not read by Helm and has no effect on the
# chart itself.
#
# repositoryID is generated by Artifact Hub when the OCI repository
# ghcr.io/eenchev/charts/prometheus-universal-exporter is registered there. Until that has happened it
# is the placeholder below, which Artifact Hub will not accept — see
# docs/RELEASING.md for the registration steps and what to replace it with.
repositoryID: "d26dfb9b-d614-40bf-a502-12b0cfcb6ada"
owners:
  - name: Evgeni Enchev
    email: eenchev2@gmail.com
