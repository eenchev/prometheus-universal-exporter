#!/bin/sh
# Installed with `make hooks`. Refuses a commit that CI would fail on gofmt,
# golangci-lint (at the version CI pins) or go vet. Bypass once with
# `git commit --no-verify`.
set -e
cd "$(git rev-parse --show-toplevel)"
exec make --no-print-directory precommit
