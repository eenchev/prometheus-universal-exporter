Exporter is available at:
  http://{{ include "prometheus-universal-exporter.fullname" . }}:{{ .Values.service.port }}

The chart does not install Prometheus Operator CRDs. Enable monitor only when the selected ServiceMonitor or PodMonitor CRD is installed.
