Exporter is available at:
  http://{{ include "prometheus-universal-exporter.fullname" . }}:{{ .Values.service.port }}

The chart does not install Prometheus Operator CRDs. Enable serviceMonitor or podMonitor only when those CRDs are installed.
