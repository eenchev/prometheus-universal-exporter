// Package config loads and validates the configuration file, its collector
// files and the scheduled target file, reloads them when they change, and
// describes them with JSON Schemas.
package config
