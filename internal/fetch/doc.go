// Package fetch gets a collector's raw data. It holds the request types (http
// and localfile, each in its own build-tagged file), their validation and the
// probe parameters they accept, path parameters and request templates, and the
// cached HTTP transports.
package fetch
