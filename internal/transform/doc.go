// Package transform turns decoded data into metrics with a collector's metric
// rules — jq, yq, XPath, CSS, CSV, regex and Prometheus pass-through — or with
// a Python script, run in a pool of worker processes. It also checks rules,
// scripts and names when the configuration loads.
package transform
